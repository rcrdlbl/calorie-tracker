<div class="card-header">Meals Eaten Today</div>
<ul>
  <% @meals.all.each do |meal| %>
    <li><%= link_to "#{meal.user.name} had #{meal.food_quantity} #{meal.food_item.name.pluralize(meal.food_quantity)}.", user_meal_path(meal.user, meal) %></li>
  <% end %>
</ul>
