<div class="welcome">All Foods</div>
<div class="food-form-container">

</div>
  <% @food_items.each do |food_item| %>
  <div class="field"><%= link_to food_item.name, new_food_item_meal_path(food_item), class: "food-item-link" %> <span class="right"><strong>Calories per serving:</strong> <%= food_item.calories %></span></div>
  <% end %>
<div class="button">
<%= link_to "Add a Food Item", new_food_item_path, class: "btn btn-primary" %>
</div>
<div class="button">
<%= link_to "See what everyone's been eating", '/meals/eaten_today', class: "btn btn-primary" %>
</div>
